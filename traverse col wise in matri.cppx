
        for(auto i = 0; i < grid[0].size(); ++i){
            for(auto j = 0; j < grid.size(); ++j){
                int t = to_string(grid[j][i]).size(); 
                ans[i] = max(ans[i], t);
            }
        }
        return ans;
